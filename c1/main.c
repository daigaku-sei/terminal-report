// coded in Termux, tested with clang
#include<stdlib.h>
#include<stdio.h>
#include<math.h>
#include<time.h>
#include"ar_reli.c"


int
main (int args, char *argv[])
{
  printf("______\n");
  printf("\n c1: [9] => T3,I3,M1: array operations on [N]x1 of random int\n");
  printf("______");

  srand (time (0));
  printf ("Try to guess rnd number, \
that will be generated by this program...\nGuess number is ?=");
  int guess_number;
  int exitLoop = 0;
  while (!exitLoop) {
      printf("Enter a number to search (default = random): ");
      if (scanf("%d", &guess_number) != 1) {
          printf("No valid int - proceeding to random the guess\n");
          guess_number = rndmg(-100, 100);
          exitLoop = 1;
      }
      else {
          printf("Guess is acknowledged!\n");
          exitLoop = 1;
      }
  }
  printf ("%d", guess_number);
  printf ("The size of an array is randomed!");
  int size = rndmg (5, 20);
  int matrix[size];
  introduction (matrix, size, guess_number);
  imprinted (matrix, size);
  inception (matrix, size);

  // search for a while, enter 666 to exit
  int num;
  int exitLoop = 0;
  while (!exitLoop) {
      printf("Enter a number to search (enter 666 to exit): ");
      scanf("%d", &num);
  
      if (num == 666) {
          exitLoop = 1;
      }
  
      searchArray(matrix, size, num);
}

  return 0;
}
